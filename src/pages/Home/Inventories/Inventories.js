import React, { useState } from 'react';

const Inventory = () => {
    // const [items] = useState();
    // const homeItems = (items.slice(6));
    

    // const { id } = useParams();
    // const navigate = useNavigate();

    // const getCategories = () => {
    //     navigate('/myItems')
    // }

    return (
        <div className='text-center my-2'>
            <h2>Inventory Items</h2>
            
            {/* {homeCategories.length === 10 && <button onClick={() => getCategories(id)} className='btn btn-success m-4'>All Categories</button>} */}
        </div>
    );
};

export default Inventory;